Typing needs for user (receive)
Typing needs for agent (receive + send)
Auto-connect (click the "forbind" btn) to user when refreshing. (maybe set active?)
Send message for agent/user (receive + send)

Actions:
    session:connect
    session:login
    room:join
    room:leave
    message:send
    message:receive
    conversation:list (only agent)
    conversation:join (only agents)
    conversation:leave

Chat rooms:
    Refactor to use routes
